manifestVersion: 1

name: "@sntxrr/scaleway-inference"
version: "2026.07.18.1"
description: "Manage a Scaleway Managed Inference deployment — sync state, create, update, and delete dedicated model serving deployments, and discover deployments in a region, via the Managed Inference API with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_inference.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - ai
  - inference
  - llm
