manifestVersion: 1

name: "@sntxrr/scaleway-iam"
version: "2026.07.17.1"
description: "Manage a Scaleway IAM application — sync, create, update, delete, and list applications, plus list API-key metadata and policies, via the global IAM API (/iam/v1alpha1) with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_iam.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - security
  - iam
  - identity
