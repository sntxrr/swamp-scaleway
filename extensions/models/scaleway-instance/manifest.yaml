manifestVersion: 1

name: "@sntxrr/scaleway-instance"
version: "2026.07.18.1"
description: "Manage a Scaleway Instance compute server — sync current state, issue power actions (poweron/poweroff/reboot/stop_in_place/terminate), and discover servers in a zone, via the Instance API with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_instance.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - compute
  - instance
  - server
  - infrastructure
