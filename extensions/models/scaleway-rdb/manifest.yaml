manifestVersion: 1

name: "@sntxrr/scaleway-rdb"
version: "2026.07.19.1"
description: "Manage a Scaleway Managed Database (RDB) instance — sync state, create, update, and delete PostgreSQL/MySQL instances, and discover instances in a region, via the RDB API with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_rdb.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - database
  - postgresql
  - mysql
  - rdb
