manifestVersion: 1

name: "@sntxrr/scaleway-vpc"
version: "2026.07.17.1"
description: "Manage a Scaleway VPC — sync current state, create, update and delete VPCs, and discover VPCs and Private Networks in a region, via the VPC API with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_vpc.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - network
  - vpc
  - private-network
