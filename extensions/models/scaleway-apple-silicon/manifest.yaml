manifestVersion: 1

name: "@sntxrr/scaleway-apple-silicon"
version: "2026.07.17.1"
description: "Manage a Scaleway Apple silicon (Mac mini) server — sync state, create, update, delete (idempotent), reboot, and discover servers in a zone, via the Apple silicon API with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_apple_silicon.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - compute
  - apple-silicon
  - mac
