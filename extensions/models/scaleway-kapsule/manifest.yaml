manifestVersion: 1

name: "@sntxrr/scaleway-kapsule"
version: "2026.07.17.1"
description: "Manage a Scaleway Kubernetes Kapsule cluster — sync state, create, update, upgrade, and delete clusters, discover clusters and node pools in a region, and fetch the kubeconfig, via the Kubernetes API with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_kapsule.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - platform
  - kubernetes
  - kapsule
