manifestVersion: 1

name: "@sntxrr/scaleway-public-gateway"
version: "2026.07.18.1"
description: "Manage a Scaleway Public Gateway — sync current state, create, update, delete, and discover Public Gateways in a zone, via the zoned Public Gateway v2 API with X-Auth-Token auth."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

models:
  - scaleway_public_gateway.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - network
  - public-gateway
  - vpc
