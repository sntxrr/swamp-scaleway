manifestVersion: 1

name: "@sntxrr/scaleway-inventory"
version: "2026.07.17.1"
description: "Consolidated inventory report across all @sntxrr/scaleway-* models — counts per service, per region/zone, per status, plus resource ids, names, and statuses, discovered from stored sync/list snapshots."
repository: "https://github.com/sntxrr/swamp-scaleway"

paths:
  base: manifest

reports:
  - scaleway_inventory.ts

additionalFiles:
  - README.md
  - LICENSE.md

labels:
  - scaleway
  - report
  - inventory
